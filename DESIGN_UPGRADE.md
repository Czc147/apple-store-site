# UI 升级设计规范

> 交付对象：Claude / 前端实现者  
> 改动规模：大型 UI 升级  
> 核心方向：Apple 白色画廊 + Premium 彩色玻璃 + 专辑式选购合集 + iOS 交易体验  
> 约束：不改后端、不改 API、不改数据库、不改业务逻辑、不改变现有功能语义。

---

## 1. 设计目标

本次升级不是简单“换颜色”或“加圆角”，而是将前台商店从现有的移动端交易界面，升级为更有产品叙事感的现代软件订阅商店。

### 1.1 设计定位

一句话定位：

> 白色画廊承载商品叙事，彩色玻璃承载高级订阅，专辑合集承载选购探索，iOS 交互承载购买流程。

整体气质应接近：

- Apple 产品官网的留白、排版和图片主导叙事；
- 海外软件订阅产品的长条方案卡、价格层级和权益矩阵；
- Apple Music / 专辑陈列式的封面堆叠与共享元素展开动画；
- iOS 原生 App 的底部导航、底部弹层、胶囊按钮和触觉式反馈。

### 1.2 视觉分层

前台分为四个视觉层级：

| 层级 | 名称 | 使用范围 | 视觉特点 |
| --- | --- | --- | --- |
| L0 | Gallery White | 首页、商品叙事、普通内容 | 纯白、克制、大留白 |
| L1 | Studio Mist | 精选带、权益区、页脚 | `#f5f5f7` 浅灰背景 |
| L2 | Premium Glass | 订阅主卡、高级会员、活动主推 | 彩色底斑 + 白色磨砂玻璃 |
| L3 | Transaction Layer | 结算、购买、兑换、表单 | iOS 式白底、发丝线、清晰控件 |

### 1.3 设计原则

1. **商品和封面是主角。** UI 只做轻量引导，避免大面积渐变、重阴影和复杂装饰。
2. **普通功能保持安静。** 商品流、列表、表单、社区内容不使用彩色玻璃。
3. **高级感集中呈现。** 订阅、会员、活动主推、优惠券强展示才使用 Premium Glass。
4. **动效服务连续性。** 动画必须表达空间关系、状态变化或内容来源，不做无意义循环。
5. **移动端优先。** 所有新组件先保证 iPhone 体验，再扩展桌面布局。
6. **交易路径不变。** 商品选择、收藏、结算、订阅、兑换等业务流程保持现有逻辑。
7. **数据结构不动。** 页面继续消费现有 API 和字段，只重做展示层与前端交互。

### 1.4 明确非目标

- 不新增后端接口。
- 不修改 Supabase 表结构。
- 不调整商品排序规则、首页板块顺序或数据返回逻辑。
- 不改变现有 6 个底部 Tab 的业务含义。
- 不重做后台管理端视觉，后台仅可复用基础控件和 token。
- 不引入需要新数据字段的设计强依赖；若缺少数据，组件应优雅降级。

---

## 2. 设计 Token

实现时优先收敛到 Tailwind config 与全局 CSS token，不要在组件中散写颜色、圆角、动效时长和阴影。

## 2.1 色彩

### 基础色

| Token | 值 | 用途 |
| --- | --- | --- |
| `gallery-white` | `#ffffff` | 页面主背景、普通卡片、叙事区 |
| `studio-mist` | `#f5f5f7` | 横向精选带、权益区、页脚 |
| `paper-frost` | `#fafafc` | 展开导航层、极浅辅助面板 |
| `control-gray` | `#e6e6e8` | 禁用控件、弱化边缘 |
| `hairline-silver` | `#d6d6d6` | 1px 分隔线、弱化描边 |
| `ink` | `#1d1d1f` | 主标题、主正文 |
| `slate` | `#707070` | 次级说明 |
| `steel` | `#86868b` | 输入描边、未选中状态 |

### 功能色

| Token | 值 | 用途 |
| --- | --- | --- |
| `apple-blue` | `#0071e3` | 主购买按钮、强转化按钮 |
| `apple-blue-active` | `#0066cc` | 按压态、文字链接 |
| `apple-blue-hover` | `#0077ed` | 桌面 hover |
| `apple-blue-soft` | `#e8f1fb` | 选中背景、极轻提示 |
| `success` | `#1d8a3e` | 成功状态 |
| `danger` | `#d70015` | 危险、错误、取消 |
| `launch-orange` | `#b64400` | 新品、上线、今日更新等裸文本标签 |

### Premium 色斑

Premium Glass 底部色斑建议：

| 名称 | 值 | 用途 |
| --- | --- | --- |
| `prism-blue` | `#0066cc` | 玻璃底斑主色 |
| `prism-cyan` | `#32ade6` | 冷色反光 |
| `prism-green` | `#34c759` | 权益感色斑 |
| `prism-yellow` | `#ffcc00` | 高亮反射 |
| `prism-orange` | `#ff9500` | 暖色反射 |
| `prism-red` | `#ff375f` | 小面积警示性高级感 |
| `prism-purple` | `#bf5af2` | 订阅高级感主色 |
| `prism-indigo` | `#4f46e5` | 会员 / 高级方案色斑 |

这些颜色只允许用于：

- Premium Glass 底部色斑；
- TabBar 彩虹反射动画；
- 极少数主视觉点缀。

不允许用于：

- 常态大面积背景；
- 普通商品卡；
- 表单；
- 长文本内容区；
- 后台管理界面。

## 2.2 字体

继续使用系统字体栈，不引入需要额外授权或下载的 SF Pro 字体。

推荐字体栈：

```text
-apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display",
  "Helvetica Neue", "Inter", "Segoe UI", Roboto, "Noto Sans SC",
  "PingFang SC", "Microsoft YaHei", sans-serif
```

### 字级体系

前台建议使用两套层级：

#### App 控件级

| 名称 | 字号 / 行高 | 字重 | 用途 |
| --- | --- | --- | --- |
| `micro` | `10px / 14px` | 500 | 角标、Tab 标签 |
| `2xs` | `11px / 15px` | 400 | 时间、法律说明 |
| `xs` | `12px / 16px` | 400 | 按钮、胶囊、小标签 |
| `sm` | `13px / 18px` | 400 | 辅助信息 |
| `base` | `14px / 20px` | 400 | 正文 |
| `md` | `15px / 22px` | 500 | 列表标题、强调正文 |
| `lg` | `17px / 24px` | 600 | 区块标题 |
| `xl` | `22px / 28px` | 600 | 页面标题、价格 |
| `2xl` | `28px / 34px` | 600 | 大标题、移动 Hero |

#### 编辑叙事级

| 名称 | 字号 / 行高 | 字重 | 用途 |
| --- | --- | --- | --- |
| `editorial-sm` | `14px / 20px` | 400 | 权益短句 |
| `editorial-base` | `17px / 24px` | 400 | 正文叙事 |
| `editorial-title` | `32px / 38px` | 600 | 平板标题 |
| `editorial-display` | `48px / 52px` | 600 | 桌面大标题 |
| `editorial-hero` | `72px / 76px` | 600 | 首页主标题，按视口收缩 |

中文排版注意：

- 中文标题不要直接使用 `-1.2px` 这类英文负字距，避免拥挤；
- 英文数字标题可使用轻负字距；
- 中文正文保持自然字距；
- 数字价格可用 tabular numbers，保证列表对齐。

## 2.3 圆角

| Token | 值 | 用途 |
| --- | --- | --- |
| `radius-chip` | `10px` | 小图标、小按钮、输入内件 |
| `radius-input` | `12px` | 输入框 |
| `radius-card` | `20px` | 普通商品卡 |
| `radius-card-lg` | `24px` | 活动卡、长内容卡 |
| `radius-hero` | `28px` | Hero、专辑、玻璃卡、大媒体卡 |
| `radius-premium` | `32px` | 订阅大卡、高级玻璃卡 |
| `radius-pill` | `9999px` | 按钮、胶囊、搜索框 |

## 2.4 阴影与边缘

Apple 画廊区域的默认原则是：**用背景差异和圆角表达层级，少用投影。**

| Token | 值 | 用途 |
| --- | --- | --- |
| `shadow-none` | 无 | 叙事卡、Feature Media Card、浮动价格卡 |
| `shadow-card` | `0 1px 2px rgba(0,0,0,.04), 0 2px 12px rgba(0,0,0,.05)` | 普通商品卡 |
| `shadow-card-hover` | `0 12px 32px rgba(0,0,0,.10)` | 桌面 hover |
| `shadow-stack` | `0 18px 40px rgba(0,0,0,.16)` | 专辑堆叠卡后层投影 |
| `shadow-glass` | `0 44px 88px -32px rgba(67,56,202,.30), 0 16px 40px -20px rgba(67,56,202,.16), inset 0 1px 0 rgba(255,255,255,.55)` | Premium Glass |
| `shadow-tabbar` | `0 -1px 0 rgba(0,0,0,.06)` | 底部导航发丝线 |

禁止：

- 给所有卡片统一加重阴影；
- 给白色叙事卡加渐变边框；
- 用阴影补偿排版层级不清的问题。

## 2.5 间距与布局

### 页面容器

| 场景 | 移动端 | 平板 | 桌面 |
| --- | --- | --- | --- |
| 左右留白 | `16px` | `24px` | `40px` |
| 最大宽度 | `100%` | `768px` | `1024px` 或 `1120px` |
| 区块间距 | `40px` | `56px` | `72px–90px` |
| 相关元素间距 | `12px–20px` | `16px–20px` | `20px–24px` |

首页可以在桌面突破常规 `1024px` 容器，让 Hero 和图片叙事区域达到 `1120px`，但交易列表仍保持收拢宽度。

### 网格

- 移动端：单列；
- 平板：两列商品卡或 8 列编辑布局；
- 桌面：12 列编辑布局。

页面排布顺序由后端现有配置决定。前端不得重新定义板块顺序、商品排序或业务推荐逻辑。

---

## 3. 材质规范

## 3.1 Gallery White

用途：

- 首页主背景；
- 每日推荐 Hero；
- 商品叙事区；
- 普通白色卡片；
- 长篇说明内容。

特征：

- 纯白背景；
- 少量发丝线；
- 图像大面积展示；
- 无边框、无投影为默认；
- 文字和图片承担视觉重心。

## 3.2 Studio Mist

用途：

- 横向精选带；
- 订阅权益说明；
- 社区精选；
- 页脚；
- 图片较少的中性区域。

特征：

全宽背景 `#f5f5f7`，内部白色卡片使用 28px 圆角、无阴影。

## 3.3 Premium Glass

Premium Glass 是本次升级的高级视觉层，不是全站默认材质。

### 适用组件

- 订阅主推卡；
- 高级会员卡；
- 年度 / 团队 / 推荐方案卡；
- 活动主视觉；
- 每日推荐信息层；
- 兑换成功或优惠券强展示。

### 不适用组件

- 普通商品卡；
- 商品列表行；
- 表单；
- 社区普通帖；
- 愿望单普通行；
- 后台管理界面；
- 长文本说明区。

### 结构

Premium Glass 分为三层：

1. **色斑层：** 大型柔和彩色斑点，位于玻璃面板底部；
2. **玻璃层：** 半透明白 + blur + saturate；
3. **内容层：** 深色文字、图标、价格和按钮。

### 参数建议

```text
色斑层：
  2–3 个椭圆色斑
  尺寸：40%–80% 卡片宽度
  透明度：24%–48%
  模糊：40px–80px

玻璃层：
  background: rgba(255,255,255,.42–.58)
  backdrop-filter: blur(22px–28px) saturate(1.55–1.8)
  border: 1px solid rgba(255,255,255,.45)
  radius: 28px–32px

降级：
  不支持 backdrop-filter 时使用 rgba(255,255,255,.94)
```

### 可读性要求

- 玻璃层透明白不得低于 `0.42`；
- 正文文字必须为 `#1d1d1f`；
- 次级文字至少为 `#3a3a3c`；
- 蓝色按钮内文字为白色；
- 色斑不得直接承载文字；
- 色斑不得覆盖重要操作按钮。

---

## 4. 基础组件规范

## 4.1 Button

### 主按钮 · Primary Pill

```text
背景：#0071e3
文字：#ffffff
高度：44px–52px
圆角：9999px
字号：14px–15px
字重：600
按压：#0066cc，scale(.97)
hover：#0077ed
```

用途：

- 购买；
- 结算；
- 订阅；
- 兑换；
- 登录。

### 次级按钮 · Outlined Pill

```text
背景：transparent
描边：1px #86868b
文字：#1d1d1f
高度：40px–48px
圆角：9999px
hover：rgba(0,0,0,.04)
```

用途：

- 查看详情；
- 了解更多；
- 展开说明；
- 取消。

### 文本链接 · Text Link

```text
颜色：#0066cc
hover：#0077ed
active：#003b66
```

用途：

- 标题旁的“了解更多”；
- 常见问题；
- 条款链接。

### 禁止

- 不使用大渐变按钮；
- 不使用重投影按钮；
- 不把蓝色按钮当作普通卡片背景；
- 不给次级操作使用蓝色填充。

## 4.2 Surface / Card

所有卡片必须声明视觉等级，不允许随手组合边框、阴影和圆角。

| 组件 | 背景 | 圆角 | 边框 | 阴影 |
| --- | --- | --- | --- | --- |
| `GalleryCard` | 白 | 28px | 无 | 无 |
| `ProductCard` | 白 | 20px–28px | 可选发丝线 | 轻 |
| `FeatureMediaCard` | 白 | 28px | 无 | 无 |
| `EditorialBlock` | 白 | 无 | 无 | 无 |
| `PremiumGlassCard` | 玻璃 | 32px | 白高光 | glass |
| `ListRow` | 白 / 浅灰 | 0 或 12px | 发丝线 | 无 |

## 4.3 TextField

```text
背景：#ffffff
描边：1px #86868b
圆角：12px
高度：44px–52px
聚焦：2px rgba(0,113,227,.55)
错误：#d70015
禁用：#e6e6e8
```

要求：

- label 与错误提示必须可被屏幕阅读器识别；
- 输入框不使用彩色玻璃；
- 密码、登录、兑换等交易场景保持高对比。

## 4.4 Sheet / ActionSheet

移动端：

- 底部滑入；
- 高度按内容自适应，最高 88dvh；
- 圆角顶部 28px；
- 遮罩 `rgba(0,0,0,.4)`；
- 动画 300ms。

桌面：

- 居中 Dialog；
- 最大宽度 480px；
- 圆角 28px；
- 动画 200ms。

## 4.5 EmptyState

结构：

1. 单色轻图标；
2. 简短标题；
3. 一句解释；
4. 可选次级操作。

禁止：

- 使用夸张插画；
- 使用彩色玻璃；
- 堆叠多个按钮。

---

## 5. 专辑式选购合集

这是本次选购页的核心差异化交互。

## 5.1 概念

选购页的大单元商品不再使用普通平铺卡片，而升级为“专辑合集”形态：

- 封面像专辑一样竖向倾斜、前后层叠；
- 前封面展示后台上传的商品封面；
- 点击封面后，封面从原位置平滑放大到屏幕上方；
- 下方丝滑展开对应小单元；
- 关闭时所有元素回到原位。

这不是完整 3D 场景，而是：

> 轻透视层叠 + FLIP 共享元素过渡 + 列表 stagger 展开。

## 5.2 视觉形态

### 前封面

- 使用后台上传的商品封面；
- 比例建议 `3:4` 或 `4:5`；
- 圆角：20px–28px；
- 图片使用 `object-fit: cover`；
- 深色或透明封面下方保留白色底板。

### 后封面

- 可使用同大单元封面或商品组内小单元封面；
- 缩放至前封面的 86%–94%；
- 向后偏移 `8px–16px`；
- 轻微 `rotateY(6deg–10deg)`；
- 透明度 70%–90%；
- 不遮挡前封面主体。

### 堆叠数量

- 视觉上展示 2–4 层即可；
- 不要求真实渲染全部小单元；
- 图层数量由前端视觉决定，不依赖新后端字段。

### 文字信息

初始态显示：

1. 大单元名称；
2. 一句简介；
3. 小单元数量；
4. 展开提示或轻图标。

信息位置：

- 移动端：封面下方；
- 桌面：封面右侧或下方，避免遮挡图片。

## 5.3 交互状态

### 初始态

- 专辑封面层叠展示；
- 当前封面完整可见；
- 后层封面轻透视；
- 页面保持滚动能力。

### Hover / Focus

- 前封面亮度轻微提高；
- 阴影增强；
- 后封面向外扩散 2px–6px；
- 位移不超过 8px；
- 键盘焦点必须有清晰 outline。

### 按压态

- 前封面 `scale(.98)`；
- 阴影减弱；
- 后层保持不动。

### 展开态

- 被点击封面移动到屏幕上方舞台；
- 其他封面淡出或退散；
- 小单元列表从下方依次展开；
- 页面背景可轻微变灰，形成专注层。

## 5.4 展开动画

### 动画流程

1. 用户点击封面；
2. 测量收起态封面的位置和尺寸；
3. 测量展开态封面舞台的位置和尺寸；
4. 使用 FLIP 变换完成封面移动和放大；
5. 小单元面板出现；
6. 小单元行依次滑入。

### 封面舞台

| 设备 | 尺寸建议 |
| --- | --- |
| 移动端 | 宽度 85%–92%，比例保持 `3:4` 或 `4:5` |
| 平板 | 宽度 55%–70% |
| 桌面 | 宽度 380px–460px，可左右布局 |

### 时间参数

| 动画 | 时长 |
| --- | --- |
| 封面移动 / 放大 | `320ms–420ms` |
| 遮罩淡入 | `200ms` |
| 小单元面板展开 | `260ms–320ms` |
| 单行 stagger | `40ms–60ms` |
| 关闭逆动画 | `280ms–360ms` |

### 缓动

```text
封面 / 面板：
  cubic-bezier(0.32, 0.72, 0, 1)

封面轻微落位：
  cubic-bezier(0.34, 1.56, 0.64, 1)

普通内容：
  cubic-bezier(0.4, 0, 0.2, 1)
```

封面可以有极轻过冲，但不做夸张弹跳。

### 性能要求

- 只动画 `transform` 和 `opacity`；
- 不动画 `width`、`height`、`top`、`left`；
- 图片加载完成前不触发大位移动画；
- 展开区域使用 `contain` 或局部重渲染，避免整页重排；
- 长列表可懒渲染，但首屏小单元应立即可见。

## 5.5 小单元展开区

展开后的区域结构：

1. 封面舞台；
2. 大单元标题与简介；
3. 价格 / 起价 / 说明；
4. 小单元列表；
5. 关闭按钮；
6. 可选：收藏、购买入口。

### 小单元行

结构：

```text
[小封面] [名称 + 简介] [价格] [收藏] [购买]
```

移动端：

- 主内容单列；
- 操作按钮右侧或底部固定；
- 点击区域不小于 44px。

桌面：

- 列表左侧；
- 封面和标题右侧；
- 操作集中；
- 不使用大段说明文字。

### 小单元行动画

```text
初始：opacity 0，translateY(10px)
结束：opacity 1，translateY(0)
stagger：40ms–60ms
```

## 5.6 关闭行为

- 点击关闭按钮、遮罩或返回；
- 小单元面板先收起；
- 封面再回到原合集位置；
- 页面滚动位置必须恢复；
- 避免用户关闭后跳到页面顶部。

## 5.7 可访问性

- 专辑卡必须是可操作元素，不只依赖图片点击；
- 提供 `aria-expanded`；
- 展开后焦点移到面板标题或关闭按钮；
- 关闭后焦点返回原卡片；
- 面板支持键盘 Escape 关闭；
- `prefers-reduced-motion: reduce` 时改为普通淡入淡出，不做封面飞行。

## 5.8 数据约束

- 继续使用现有大单元 / 小单元数据；
- 封面继续使用后台上传字段；
- 不新增“堆叠封面”字段；
- 堆叠层数、透视角度和偏移由前端视觉规则决定；
- 后台已规定的排布顺序必须原样保留，前端不得重新排序。

---

## 6. 订阅长条大卡

## 6.1 组件名称

建议实现为：

```text
SubscriptionMegaCard
```

## 6.2 使用场景

- 订阅首页主方案；
- 月付 / 年付 / 团队方案；
- 高级会员；
- 推荐套餐；
 -活动订阅套餐。

## 6.3 布局

### 移动端

结构：

```text
[标签]
[方案名称]
[大价格 + 周期]
[核心权益 4–6 条]
[服务图标区]
[主按钮]
[说明 / 条款]
```

尺寸：

- 宽度：100%；
- 内边距：24px；
- 圆角：32px；
- 高度按内容自适应。

### 平板

结构：

```text
左侧：标题、价格、标签
右侧：权益矩阵
底部：CTA
```

宽度：约 8 列。

### 桌面

结构：

```text
12 列中占 8–10 列
高度：360px–480px

左列 4–5 份：
  标签、方案名、大价格、周期、主按钮

右列 6–7 份：
  权益矩阵、服务图标、说明
```

## 6.4 视觉

普通方案：

- 白底或 `#f5f5f7`；
- 28px–32px 圆角；
- 发丝线；
- 无重阴影。

推荐方案 / 高级方案：

- Premium Glass；
- 底部柔和色斑；
- 白高光边；
- 轻玻璃阴影；
- 顶部或角部小标签。

## 6.5 价格层级

1. **周期：** `¥39 / 月`，数字较大；
2. **原价：** 小号灰色，可删除线；
3. **换算价：** `约合 ¥3.25 / 天`，仅作辅助；
4. **条款：** 11px–12px 灰色文字。

不要把所有价格都做成同样大小。主方案价格必须是第一层级。

## 6.6 权益展示

每条权益：

```text
[轻图标] [短句]
```

规则：

- 每条不超过 12 个中文字符；
- 首屏显示 4–6 条；
- 更多权益折叠；
- 不使用大段说明。

## 6.7 服务图标区

用于展示包含的软件或服务。

结构：

```text
[圆形图标] [服务名]
```

视觉：

- 图标直径 36px–44px；
- 白色或极浅灰底；
- 最多一行 4–6 个；
- 超出显示 `+N`；
- 不使用彩色玻璃背景。

## 6.8 方案选择

月付 / 年付切换：

- 使用胶囊分段控件；
- 放在卡片顶部或卡片组上方；
- 切换时价格和权益同步更新；
- 不把切换器藏在卡片底部。

## 6.9 状态

| 状态 | 视觉 |
| --- | --- |
| 默认 | 白底 / 浅灰 |
| 推荐 | Premium Glass + 标签 |
| 当前方案 | 蓝色边线或顶部选中条 |
| 不可用 | 灰色文字、禁用按钮 |
| 加载 | 骨架屏，不显示半真实价格 |

---

## 7. TabBar 彩虹反射效果

## 7.1 概念

底部 6 个 Tab 保持常态克制：

- 白色毛玻璃导航；
- 未选中灰色图标；
- 选中 Apple Blue。

当用户切换 Tab 时，被选中图标内部播放一次短暂的彩虹反射，然后回到 Apple Blue 常态。

这不是长期彩色图标，而是切换瞬间的玻璃反光记忆点。

## 7.2 常态

```text
背景：rgba(255,255,255,.75) + blur(20px) + saturate(1.8)
顶边：1px rgba(0,0,0,.06)
未选中图标：#86868b
选中图标：#0071e3
标签：10px / 500
点击区域：≥44px
```

## 7.3 切换动画

触发时机：

- 用户点击新 Tab；
 -路由 pathname 变化；
- 页面进入动画开始。

播放对象：

仅当前被激活的图标，不联动其他 5 个 Tab。

动画：

```text
时长：250ms–300ms
重复：一次
方向：左下 → 右上
范围：仅图标内部
最终状态：Apple Blue
```

彩虹色带：

```text
#0066cc
#32ade6
#34c759
#ffcc00
#ff9500
#ff375f
#bf5af2
```

## 7.4 视觉实现方式

推荐：

- 使用彩虹渐变作为反射层；
- 用 icon mask 裁剪到图标形状内部；
- 动画 `background-position` 或渐变位移；
- 配合 `opacity 0 → .65 → 0`；
- 图标本体保持可识别。

不建议：

- 直接把图标颜色改成彩虹渐变并长期保留；
- 6 个 Tab 同时闪烁；
- 依次播放扫光；
- 彩虹反射覆盖 Tab 标签；
- 与角标弹跳动画叠加。

## 7.5 辅助反馈

点击时：

```text
icon scale: .92 → 1
duration: 180ms
ease: cubic-bezier(0.4, 0, 0.2, 1)
```

可选：

图标下方一条 `2px` 指示线，透明度不超过 0.5，使用蓝到紫的轻渐变。

## 7.6 桌面与无障碍

桌面 hover：

- 不播放完整彩虹；
- 只显示 6%–10% 白色高光；
- 或仅改变图标颜色。

`prefers-reduced-motion: reduce`：

- 直接跳过反射动画；
- 保留灰到蓝的颜色切换；
- 不做位移或扫光。

---

## 8. 页面设计

## 8.1 选购页

### 目标

将首页从“普通商品流”升级为“专辑合集探索 + 每日推荐 + 编辑式板块”。

### 页面结构

```text
公告条
首页大标题 / 副标题
每日推荐 Hero
专辑式商品合集流
辅助板块
客服悬浮入口
底部 TabBar
```

板块顺序和内容来自后端配置，前端只负责呈现。

### 每日推荐 Hero

视觉：

- 白色 Gallery；
- 大图；
- 小 kicker；
- 大标题；
- 副标题；
- 蓝色胶囊按钮。

移动端：

- 上下布局；
- 图像优先；
- 标题在图下方。

桌面：

- 左文右图或全宽舞台；
- 标题使用编辑级字号；
- 图片允许裁切，但主体完整。

### 专辑合集流

每个大单元使用 `AlbumStackCard`。

初始态：

- 专辑封面层叠；
- 显示名称、简介和数量；
- 可横向或按后台规定布局呈现。

展开态：

- 封面移动到舞台；
- 小单元丝滑展开；
- 交易操作保持现有逻辑。

### 普通小单元

如果某些场景不适合专辑堆叠，可使用安静列表：

```text
[小封面] [标题 + 简介] [价格] [收藏 / 购买]
```

不要让所有区域都变成重视觉组件。

## 8.2 愿望单页

### 目标

从普通购物车列表升级为“收藏画廊 + 清晰结算”。

### 结构

```text
页面标题
统计摘要
商品列表
悬浮结算条
结算 Sheet
```

### 商品行

- 封面较大；
- 名称和价格清晰；
- 数量控制弱化但易点；
- 删除操作放在右侧或滑动操作；
- 不使用玻璃效果。

### 悬浮结算条

```text
背景：白色毛玻璃
圆角：9999px 或 24px
高度：56px–64px
内容：数量 + 总价 + 结算按钮
```

移动端固定在 TabBar 上方，避让安全区和 TabBar 高度。

## 8.3 订阅页

### 目标

呈现软件订阅价值和方案差异，减少“买卡密”的工具感。

### 结构

```text
页面标题
价值叙述区
月付 / 年付切换
SubscriptionMegaCard 主方案
普通方案卡
权益对比
常见问题
```

### 主视觉

顶部可以使用 Premium Glass 或白色编辑式 Hero。

建议：

- 标题短而明确；
- 价格层级清楚；
- 主 CTA 稳定可见；
- 权益以短句和图标呈现；
- 条款弱化但不消失。

### 权益对比

移动端：

- 使用分组列表，不强行塞表格；
- 每组显示 3–4 项；
- 相同项弱化显示。

桌面：

- 可使用对比表；
- 当前列高亮；
- 表格线使用发丝线。

## 8.4 活动页

### 目标

像“活动专题”而不是普通公告列表。

### 结构

```text
页面标题
活动主推卡
活动列表
筛选 / 状态
```

### 活动主推卡

- 可使用 Premium Glass；
- 大标题；
- 时间和状态；
- 背景图或活动封面；
- 主 CTA。

### 普通活动卡

- 白底或 `#f5f5f7`；
- 图文两列；
- 状态标签用裸文本或极轻背景；
- 不使用玻璃。

## 8.5 社区页

### 目标

阅读优先，减少装饰。

### 结构

```text
页面标题
发帖入口
内容流
评论区 / Sheet
```

视觉：

- 白底；
- 用户信息、正文、图片层级清楚；
- 操作按钮弱化；
- 图片圆角 16px–20px；
- 不使用 Premium Glass。

## 8.6 我的库页

### 目标

让已购内容像“个人馆藏”，同时保持高效管理。

### 结构

```text
个人资料卡
通知入口
优惠券卡
已购内容分组
内容详情
```

### 个人资料卡

- 可用轻玻璃，但必须保持可读；
- 头像、昵称、状态清晰；
- 编辑入口弱化。

### 已购内容

- 封面 + 名称 + 有效期 / 状态；
- 状态使用文字色，不做大色块；
- 管理操作放在右侧或 Sheet 中。

## 8.7 兑换页

### 目标

交易场景优先，动效只用于反馈。

### 结构

```text
页面标题
输入卡
兑换按钮
结果反馈
```

视觉：

- 白底；
- 输入框清晰；
- 结果卡可用极轻彩色玻璃，但不影响错误状态识别；
- 成功态短动画不超过 500ms。

## 8.8 登录页

视觉：

- 白色或浅灰；
- 表单居中；
- 交易控件优先；
- 不使用彩色玻璃。

要求：

- 输入框大且易点；
- 登录按钮使用主蓝色胶囊；
- 错误信息清楚；
- 不为了视觉牺牲可用性。

## 8.9 后台管理端

本轮不进行完整视觉重塑。

允许：

- 复用颜色、字体、圆角、按钮、输入框 token；
- 修正明显不一致；
- 统一间距和表格样式。

禁止：

- 使用 Premium Glass；
- 使用专辑堆叠动画；
- 使用彩虹反射；
- 大规模重写后台组件。

---

## 9. 动效系统

## 9.1 基本时长

| 类型 | 时长 |
| --- | --- |
| hover | `150ms` |
| 按压 | `150ms–180ms` |
| 页面进入 | `200ms` |
| 颜色 / 透明度 | `180ms–220ms` |
| Sheet | `300ms` |
| 专辑封面展开 | `320ms–420ms` |
| 小单元展开 | `260ms–320ms` |

## 9.2 缓动

```text
fast / base:
  cubic-bezier(0.4, 0, 0.2, 1)

sheet / cover:
  cubic-bezier(0.32, 0.72, 0, 1)

micro pop:
  cubic-bezier(0.34, 1.56, 0.64, 1)
```

`micro pop` 只用于：

- TabBar 角标；
- 封面轻微落位；
- 极少数确认反馈。

## 9.3 页面切换

保留现有方向：

```text
opacity 0 → 1
translateY 4px → 0
duration 200ms
```

禁止：

- 大幅页面旋转；
- 离场拦截造成导航卡顿；
- 每个区块重复播放重动画。

## 9.4 滚动出现

用于编辑式区块：

```text
opacity 0 → 1
translateY 6px–10px → 0
duration 300ms
play once
```

要求：

- 只在进入视口时播放一次；
- 不循环；
- 不让主要内容因动画延迟不可读；
- 列表首屏不做过多 stagger。

## 9.5 骨架屏

```text
背景：#ededf0 → #f7f7f9 shimmer
时长：1.4s
循环：加载期间循环
```

骨架结构必须与最终结构同构，特别是：

- 专辑卡；
- Hero；
- 订阅大卡；
- 愿望单行。

避免加载完成后大幅跳变。

## 9.6 减弱动态效果

`prefers-reduced-motion: reduce` 时：

- 页面切换仅保留淡入；
- Sheet 直接显示或使用极短淡入；
- 专辑封面不做飞行；
- TabBar 不播放彩虹反射；
- shimmer 停止或大幅减弱；
- 保留状态变化和颜色反馈。

---

## 10. 响应式设计

## 10.1 移动端

- 单列布局；
- 底部 TabBar 固定；
- 底部弹层优先；
- 所有点击目标 ≥44px；
- 安全区正确避让；
- 长卡片内容上下排列。

## 10.2 平板

- 内容最大宽度 768px；
- 商品卡可两列；
- 订阅卡左右分栏；
 -专辑合集可横向滚动或两列；
- TabBar 可保留，也可按现有交互保持一致。

## 10.3 桌面

- 编辑式布局；
 顶部导航或保留底部导航视现有信息架构决定；
- Hero 可全宽；
- 交易区收拢；
- hover 状态克制；
- 弹层居中。

桌面不建议强行模拟手机 App。应根据页面内容提供更舒适的浏览布局，但交易流程和路由不变。

---

## 11. 图片与资产要求

### 商品封面

- 建议最小宽度：800px；
- 理想宽度：1200px–1600px；
- 比例：接近 3:4 或 4:5；
- 格式：WebP / AVIF 优先，JPG 兜底；
- 避免极端透明背景。

### 每日推荐封面

- 建议比例：16:9 或 21:9；
- 主体居中或右侧；
- 留出文字区域。

### 订阅卡

- 不强依赖新图片；
- 主要靠排版、色斑和玻璃层表达高级感；
- 如使用软件图标，必须使用现有可授权资产。

### 降级

- 无封面：使用品牌浅灰底 + 名称缩写；
- 低分辨率封面：限制最大展示尺寸；
- 加载失败：显示轻量占位，不阻断交易；
- 图片加载完成前不做复杂布局动画。

---

## 12. 实现步骤

建议按以下顺序实施，避免一次性大改导致不可回退。

### Phase 1 · Token 与基础组件

1. 收敛颜色、字号、圆角、阴影、动效 token；
2. 升级 Button、Surface、TextField、Sheet、EmptyState；
3. 保留现有业务 props 和调用逻辑；
4. 建立 Premium Glass 基础组件；
5. 建立 reduced-motion 适配。

### Phase 2 · TabBar 彩虹反射

1. 保留 6 个 Tab；
2. 增加激活图标彩虹反射动画；
3. 处理 hover、active、keyboard focus；
4. 处理 reduced-motion；
5. 确保角标动画不与反射叠加。

### Phase 3 · 选购页专辑合集

1. 新建 `AlbumStackCard` 视觉层；
2. 使用现有商品封面；
3. 实现封面 FLIP 展开；
4. 实现小单元 stagger 展开；
5. 接回现有收藏、购买、展开业务逻辑；
6. 处理滚动恢复、焦点恢复和键盘操作。

### Phase 4 · 首页 Hero 与叙事区

1. 重做每日推荐视觉；
2. 调整首页标题和区块间距；
3. 建立 Gallery White / Studio Mist 分层；
4. 优化骨架屏。

### Phase 5 · 订阅长条大卡

1. 实现 `SubscriptionMegaCard`;
2. 建立移动 / 平板 / 桌面布局；
3. 实现价格层级和权益矩阵；
4. 使用 Premium Glass 只突出推荐方案；
5. 保持购买逻辑不变。

### Phase 6 · 其他前台页面

1. 愿望单；
2. 活动；
3. 社区；
4. 我的库；
5. 兑换；
6. 登录。

### Phase 7 · 细节验收

1. 移动端和桌面端截图检查；
2. 动效性能检查；
3. 可访问性检查；
4. 加载、空态、错误态检查；
5. 回归交易流程。

---

## 13. 验收标准

## 13.1 视觉验收

- 首页第一屏能明显感知“白色画廊 + 专辑合集”升级；
- 普通商品卡没有过度装饰；
- Premium Glass 只出现在订阅、高级会员和主推场景；
- 订阅方案卡是长条大卡，价格和 CTA 层级清楚；
- TabBar 常态克制，切换时有彩虹反射记忆点；
- 全站颜色、圆角、字号、阴影没有散写。

## 13.2 交互验收

- 点击专辑封面后，封面从原位置平滑移动到展开舞台；
- 小单元依次展开，过程无明显跳变；
- 关闭后封面回到原位置，滚动位置不丢失；
- 收藏、购买、结算逻辑全部可用；
- TabBar 6 个页面切换正常；
- 底部弹层和键盘交互正常。

## 13.3 性能验收

- 展开动画主要使用 transform / opacity；
- 无明显长任务和掉帧；
- 首屏不因动画阻塞内容；
- 图片懒加载正常；
- 骨架屏与最终布局差异小。

## 13.4 可访问性验收

- 专辑卡可键盘打开和关闭；
- 展开态焦点管理正确；
- Escape 可关闭展开面板；
- 关闭后焦点返回原卡片；
- 按钮和输入框有清晰 focus-visible；
- `aria-expanded` 等状态正确；
- reduced-motion 下无飞行和扫光动画。

## 13.5 业务验收

- 后台接口、数据库和权限逻辑未修改；
- 商品排序和板块排布仍由后台配置决定；
- 6 个 Tab 路由不变；
- 愿望单、结算、订阅、兑换、社区、我的库功能不缺失；
- 现有错误态和空态仍可理解。

---

## 14. Do / Don't

### Do

- 使用白色和浅灰建立呼吸感；
- 让图片和封面承担视觉重心；
- 把蓝色留给转化和链接；
- 让订阅大卡呈现方案价值；
- 让专辑合集成为选购页记忆点；
- 保持 TabBar 交易效率；
- 尊重后台排布结果；
- 动画连续、短促、可回退。

### Don't

- 不把全站改成玻璃拟态；
- 不给普通商品卡加彩虹和渐变；
- 不用大面积蓝色背景；
- 不改变后端和业务逻辑；
- 不重排后台规定的内容顺序；
- 不为了动画牺牲键盘和屏幕阅读器可用性；
- 不在低端设备上播放复杂 3D 效果；
- 不让 UI 装饰超过商品和内容本身。

---

## 15. 实现备注

1. 现有项目已经有 Apple 色、圆角、字体、TabBar 毛玻璃、Sheet 动效和 reduced-motion 基础，应在其上收敛，而不是另起一套完全不同的系统。
2. 专辑合集动画建议优先做“收起态 / 展开态”两套稳定布局，再通过 FLIP 补连续位移，避免一开始追求复杂物理动画。
3. Premium Glass 必须提供不支持 `backdrop-filter` 的实底白降级。
4. 彩虹反射必须限制在 TabBar 激活图标内部，不能扩散成全站彩色主题。
5. 所有新视觉组件都应有明确的状态：默认、hover、active、focus、loading、empty、error、disabled。
6. 实现过程中如发现现有数据不足，应采用前端降级方案，不得要求新增后端字段。

